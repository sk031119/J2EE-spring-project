// Requirements: Define job application lifecycle statuses - PENDING, APPROVED, REJECTED, CANCELLED, COMPLETED
package com.samuellaw.quick_job_system.enums;

public enum ApplicationStatus {
    PENDING,
    APPROVED,
    REJECTED,
    CANCELLED,
    COMPLETED
}
